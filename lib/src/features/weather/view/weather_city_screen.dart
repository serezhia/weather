import 'package:flutter/material.dart';

class WeatherCityScreen extends StatelessWidget {
  const WeatherCityScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
